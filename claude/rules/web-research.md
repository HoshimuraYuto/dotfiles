# Web research

## Find

- Which page to read → WebSearch

## Fetch

- Only whether a page says something → WebFetch (returns a summary, not the original text)
- The page content itself → agent-browser (follow `--help`)
