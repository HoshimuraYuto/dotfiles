# Commits

Conventional Commits, unless the repository's log says otherwise.

If "and" joins two actions, it is two commits.
Listing parallel objects of one action is fine — `set up prettier, lefthook and commitlint`.
